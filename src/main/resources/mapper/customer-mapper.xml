<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 必须是对应的dao，否则将无法找到该配置文件 -->
<mapper namespace="com.roadom.mybatisWithSpring.dao.CustomerDao">
	
	
	<select id="findAll" resultType="com.roadom.model.Customer">
		select * from customer
	</select>
	
</mapper>
